/**
 * Controllers and classes responsible for application flow (UI -> model).
 * Contains the main controller that drives the console interaction loop.
 */
package com.example.simple.jira.controller;
