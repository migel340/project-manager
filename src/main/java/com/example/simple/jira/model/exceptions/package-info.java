/**
 * Domain exception types used by repositories and controllers.
 * Contains error types used to report invalid model operations (e.g. not found).
 */
package com.example.simple.jira.model.exceptions;
