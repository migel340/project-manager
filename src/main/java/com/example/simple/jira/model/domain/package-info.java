/**
 * Domain model classes: entities and value objects (Project, Task, etc.).
 * Models are designed to be immutable and expose "with-" methods to create modified copies.
 */
package com.example.simple.jira.model.domain;
